import type { Configuration as DevServerConfiguration } from 'webpack-dev-server';
import { BuildOptions } from './types';
export declare function buildDevServer(options: BuildOptions): DevServerConfiguration;
